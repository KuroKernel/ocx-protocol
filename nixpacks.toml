# Nixpacks configuration for OCX Protocol
[phases.setup]
nixPkgs = ["go_1_24"]

[phases.build]
cmds = [
  "go mod download",
  "CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -ldflags=\"-w -s\" -o /app/bin/ocx-server ./cmd/server"
]

[start]
cmd = "/app/bin/ocx-server"
