apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: ocx-webhook
  namespace: ocx-system
  labels:
    app.kubernetes.io/name: ocx-webhook
    app.kubernetes.io/part-of: ocx-system
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: ocx-webhook
