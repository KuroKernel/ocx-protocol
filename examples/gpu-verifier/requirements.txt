# Tonight's baseline (CPU-side receipt pipeline deps — GPU deps come from
# the PyTorch nightly index).
cbor2>=5.6.5
cryptography>=42
# Model + tokenizer
transformers>=4.44.2
accelerate>=0.34.2
safetensors
# PyTorch itself is installed separately:
#   pip install --pre --index-url https://download.pytorch.org/whl/nightly/cu128 torch
# (nightly wheels carry sm_120 support for RTX 5060 Blackwell — stable cu124
# wheels do NOT)
